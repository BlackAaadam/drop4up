# Codex Prompt 01 — Project setup

Inspect the Flutter repo. Do not make changes yet.

Report:

1. current branch,
2. Flutter project structure,
3. where the app entry point is,
4. existing UI component files,
5. whether `flutter analyze` currently passes,
6. which files you propose to modify for Milestone 1.

Then, after reporting, implement only Milestone 1:

- add `flutter_inset_box_shadow: ^1.0.8` to `pubspec.yaml`,
- create `lib/ui/drop4up_tokens.dart`,
- do not build screens yet,
- run `flutter pub get`,
- run `flutter analyze`.

Keep the patch small.
