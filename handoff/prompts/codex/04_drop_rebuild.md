# Codex Prompt 04 — Drop rebuild

Build the Drop screen as a dedicated quick-entry tab.

Requirements:

- Header: Drop4Up.
- Headline: “Capture your heart.”
- Subtitle: “A sentence is enough.”
- Large central tactile Drop button using `PrimaryDropButton`.
- Soft concentric ring/ripple feel around the button.
- Entry card using `SoftSurface`.
- Text field copy: “What’s on your heart?”
- Source chips: Sermon, Prayer, Devotion, Other.
- Optional mic/camera buttons using `SoftIconButton`.
- Save Drop button using blue tactile surface.
- Bottom nav active tab is Drop.

The screen must feel fast, safe, quiet, encouraging, and not form-heavy.

After implementation:

- run `flutter analyze`,
- screenshot at 393×873 if possible,
- compare with `references/Drop.png`,
- report remaining visual gaps.
