# Codex Prompt 03 — Home rebuild

Build or refactor the Home screen using the new surface engine.

Use these requirements:

- Home is visual reflection and gentle discovery.
- Home must not include a timeline list.
- Include Drop4Up header.
- Include reflective hero title.
- Include subtitle.
- Include visual reflection card.
- Include Explore Tags with tactile chips.
- Include a calm bottom card.
- Bottom nav active tab is Home.

Use custom components:

- `SoftSurface`
- `Drop4UpTagChip`
- `SoftIconButton`
- `Drop4UpBottomNav`

Target viewport first:

```text
393×873 logical px
```

After implementation:

- run `flutter analyze`,
- run app if possible,
- capture screenshot if your environment supports it,
- compare against `references/Home.png`,
- list the top 3 visual gaps.
